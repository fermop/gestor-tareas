import { ProjectsView } from "@/features/projects/components/ProjectsView";

export const metadata = {
  title: "Mis Proyectos | Gestor de Tareas",
};

export default function ProyectosPage() {
  return <ProjectsView />;
}